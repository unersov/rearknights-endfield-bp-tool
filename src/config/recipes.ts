import type { Recipe } from '../types';

// The current CSV import provides facility metadata only. Keep this empty until
// authoritative recipe rows are available, instead of carrying stale placeholders.
export const RECIPES: Recipe[] = [];
